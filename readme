npm install bcrypt -- Usado para recodificar uma senha 'Hash' em algo mais complexo para evitar vazamentos/hackers e
    é usada para decodificar a senha 'Hash' que foi criada.

npm install dotenv --  Um arquivo de configuração criada na maquina local para passar coisas como chaves de Api´s,
    dominios de bancos de dados, usuarios e senhas dos bancos, basicamente é usado para trabalhar com aquivos
    sensiveis sendo assim cada ambiente tera um arquivo diferente, por exemplo, no local sera um arquivo, no
    sistema de produção sera outro e etc...

npm install express -- Framework usado com o Node.Js para a criação das Api´s

npm install jsonwebtoken -- Usado para criar e verificar tokens para os usuarios

npm install mongoose -- Pacote para usar o banco de dados facilitando a usabilidade do mongodb

-- Cria um servidor que sempre que a aplicação for salva ela sera resetada


Codigos de requisição

422 -- Servidor entende a requisição porem os dados nao estao sendo enviados corretamentes ou os dados estao errados
